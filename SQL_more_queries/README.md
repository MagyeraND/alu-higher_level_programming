# SQL - More queries
Advanced SQL tasks focusing on:
- User management and privileges
- Database constraints (NOT NULL, UNIQUE, PRIMARY KEY, FOREIGN KEY)
- Subqueries
- Various types of JOINs (INNER, LEFT)
- Aggregate functions with GROUP BY
